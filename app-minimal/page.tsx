export default function Page() {
  return (
    <html>
      <body>
        <main style={{fontFamily: 'Inter, system-ui, sans-serif', padding: 48}}>
          <h1>Go4it — Minimal Deploy</h1>
          <p>This is a minimal public funnel for Replit deployment.</p>
          <p>
            Keep Clerk and Stripe API routes in `app/api` if you need them — I can add those next.
          </p>
        </main>
      </body>
    </html>
  );
}
