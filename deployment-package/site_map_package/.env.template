# Go4It Sports Production Environment Variables

# Node.js Configuration
NODE_ENV=production
PORT=3000

# Database Configuration
DATABASE_USER=go4it_user
DATABASE_PASSWORD=replace_with_strong_password
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@localhost:5432/go4it_production

# API Keys
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# Email Configuration (if needed)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email_user
EMAIL_PASSWORD=your_email_password

# Admin Contact (for Let's Encrypt SSL certificates)
ADMIN_EMAIL=a.barrett@go4itsports.org
