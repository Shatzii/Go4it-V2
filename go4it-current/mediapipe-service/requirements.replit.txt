# Replit-Optimized MediaPipe Requirements
# Lighter versions for better performance on Replit's limited resources

flask==3.0.0
flask-cors==4.0.0

# MediaPipe - may fail on Replit free tier due to memory limits
# If installation fails, system will automatically fall back to GPT-4o only
mediapipe==0.10.8

# OpenCV - lightweight version
opencv-python-headless==4.8.1.78

# NumPy - essential for calculations
numpy==1.24.3

# Requests for health checks
requests==2.31.0
