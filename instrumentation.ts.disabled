// instrumentation.ts - Run once on server startup before any requests
// PostgreSQL-only setup (SQLite support removed)

export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    // Using PostgreSQL - no initialization required
    console.log('[Instrumentation] PostgreSQL database configured');
  }
}
